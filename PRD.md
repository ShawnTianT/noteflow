# Noteflow 产品需求文档

> 版本：1.3.1 | 最后更新：2026-05-03

---

## 一、产品概述

### 1.1 产品定位

Noteflow 是一款**本地优先的类 flomo 笔记应用**，运行在浏览器中，核心价值是"记录想法，随时随地"。数据存储在用户本地（IndexedDB），可选开启云端同步实现多设备访问。

### 1.2 目标用户

- 有碎片化记录需求的个人用户
- 原有 flomo 用户（可一键导入历史数据）
- 注重数据隐私、偏好本地存储的用户

### 1.3 产品形态

| 端 | 地址 | 状态 |
|---|---|---|
| 桌面端 | `localhost:8765`（本地）/ `shawntiant.github.io/noteflow/`（线上） | ✅ 已上线 |
| 手机端 | `localhost:8765/mobile/`（本地）/ `shawntiant.github.io/noteflow/mobile/`（线上） | 🔧 骨架已搭建，UI 开发中 |

### 1.4 技术栈

- **前端**：Vue 3 CDN（无构建工具，`Vue.createApp().mount('#app')`）
- **本地存储**：sql.js（浏览器内 SQLite） + IndexedDB 持久化
- **云端同步**：Supabase（自定义用户表 + pgcrypto 密码加密）
- **图片处理**：纯前端压缩 + base64/本地路径混合存储
- **部署**：GitHub Pages（从 `/docs` 目录），本地 Node.js HTTP 服务器

---

## 二、用户体系

### 2.1 账号系统

| 项目 | 说明 |
|---|---|
| 认证方式 | 自定义用户表（非 Supabase Auth），密码用 pgcrypto `crypt()` 加密 |
| 登录验证 | Supabase RPC 函数 `verify_password(username, password)` |
| 注册 | 暂不开放自助注册，新用户需管理员在 Supabase SQL Editor 手动插入 |

**已有账号**：

| 用户名 | 密码 | 说明 |
|---|---|---|
| 18134158895 | 123456 | 主账号（凌霄），已导入 1687 条 flomo 笔记 + 134 个标签 |
| liangpenghui | 123456 | 新账号，无预设数据 |

### 2.2 三种使用模式

| 模式 | 触发条件 | 数据范围 | 同步能力 |
|---|---|---|---|
| **云端模式** | 登录成功 | 当前用户云端 + 本地缓存数据 | ✅ 双向同步 |
| **本地模式** | 点击"暂不登录" | IndexedDB 本地全部数据 | ❌ |
| **离线模式** | 已登录但网络断开 | 本地缓存数据 | 断网期间累积变更，恢复后自动同步 |

### 2.3 登录页面

- 居中卡片式布局，Logo + 副标题 + 账号/密码输入框 + 登录按钮
- 登录失败显示错误信息
- 底部"暂不登录（本地模式）"链接
- 支持 Enter 键登录

---

## 三、核心功能

### 3.1 笔记输入

| 需求项 | 说明 |
|---|---|
| 输入方式 | 文本框输入，Enter 发送，Shift+Enter 换行 |
| 标签语法 | `#标签名`，空格分隔，支持多级如 `投资/策略`、`area's/跑步` |
| 标签预览 | 输入时实时识别 #标签，在输入框下方预览 |
| 图片上传 | 三种方式：📷 按钮选择文件 / 拖拽文件到输入框 / Ctrl+V 粘贴剪贴板图片 |
| 图片压缩 | 自动压缩至 maxWidth=1200、quality=0.7 |
| 图片预览 | 上传后在输入框下方显示缩略图预览 |
| 发送反馈 | 发送后清空输入框 + 图片预览，Toast 提示"已保存" |

**标签正则规则**：

```
提取正则：/#([\w\u4e00-\u9fa5']+(?:\/[\w\u4e00-\u9fa5']+)*)/g
过滤规则：纯英文+数字尾缀不判定为标签（如 campaign2），正则 /^[a-zA-Z]+\d+$/
```

标签逻辑变更需同步 4 个文件：`editor.js`（提取）、`timeline.js`（高亮）、`export.js`（导出）、`flomoImport.js`（导入解析）。

### 3.2 笔记展示（时间线）

| 需求项 | 说明 |
|---|---|
| 排列方式 | 按创建时间倒序，最新在前 |
| 卡片内容 | 笔记文本（标签高亮为可点击链接）+ 图片缩略图 + 创建时间 |
| 图片展示 | 缩略图内嵌卡片，点击放大查看 |
| 分页加载 | 每页 50 条，滚动到底部加载更多，显示"加载更多..."和"已加载全部笔记" |
| 编辑 | 点击笔记卡片弹出编辑模态框，修改内容后保存 |
| 删除 | 编辑模态框中提供删除按钮，确认后删除 |
| 标签高亮 | 文本中的 #标签 渲染为蓝色可点击链接，点击后筛选该标签 |

### 3.3 标签系统

| 需求项 | 说明 |
|---|---|
| 标签层级 | 以 `/` 分隔层级，如 `投资/策略`、`跑步/训练` |
| 树形展示 | 侧边栏标签区域：一级标签可展开/折叠，展示子标签 |
| 计数汇总 | 一级标签显示汇总计数（含所有子标签笔记数） |
| 标签筛选 | 点击标签筛选笔记，顶栏显示 `#标签名`；点击一级标签同时匹配子标签 |
| 标签置顶 | 点击 📌 或右键标签切换置顶，置顶标签排最前 + 分隔线，存储在 localStorage |
| 取消筛选 | 点击顶栏"全部笔记"或清空 currentTag |

**标签点击实现**：使用事件委托 + `data-tag` 属性，不用内联 onclick（避免单引号冲突）。

### 3.4 搜索

| 需求项 | 说明 |
|---|---|
| 搜索范围 | 笔记内容 + 标签名 |
| 分词逻辑 | 多关键词空格分隔，AND 逻辑（所有关键词都匹配才返回） |
| 实时搜索 | 输入后 300ms debounce 自动搜索 |
| 清除搜索 | 搜索框右侧清除按钮，清除输入 + 清除 debounce 定时器 |
| 与标签互斥 | 搜索时自动清除标签筛选，标签筛选时自动清除搜索 |

### 3.5 每日回顾

| 需求项 | 说明 |
|---|---|
| 入口 | 侧边栏"📖 每日回顾"按钮 |
| 逻辑 | 随机抽取 10 条笔记展示，清除当前标签筛选和搜索 |
| 数据源 | DB.getRandomNotes(10)，使用 SQLite RANDOM() 函数 |

---

## 四、数据管理

### 4.1 数据结构

**笔记（notes）**：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 笔记 ID，`Math.floor(Date.now() + Math.random() * 10000)` 生成 |
| user_id | BIGINT | 所属用户 ID |
| content | TEXT | 笔记内容（含 #标签 文本） |
| tags | TEXT / TEXT[] | 标签（本地为逗号分隔字符串，Supabase 为 JSON 数组） |
| image_paths | TEXT / TEXT[] | JSON 数组，大图路径 |
| image_data | TEXT / TEXT[] | JSON 数组，小图 base64 |
| type | TEXT | 固定 'text' |
| is_done | INTEGER / BOOLEAN | 0/1 |
| created_at | DATETIME / TIMESTAMPTZ | 创建时间 |
| updated_at | DATETIME / TIMESTAMPTZ | 更新时间（Supabase 自动触发器更新） |

**标签（tags）**：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 标签 ID |
| user_id | BIGINT | 所属用户 ID |
| name | TEXT | 标签名（如 `投资/策略`） |
| count | INTEGER | 冗余计数，加速展示 |

**用户（users）**：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGSERIAL | 用户 ID |
| username | TEXT | 用户名（UNIQUE） |
| password_hash | TEXT | pgcrypto bf 加密后的密码哈希 |
| created_at | TIMESTAMPTZ | 注册时间 |

### 4.2 图片存储策略

| 图片大小 | 存储方式 | 字段 | 说明 |
|---|---|---|---|
| < 100KB | base64 字符串 | `image_data` | 小图直接内嵌，无需额外请求 |
| ≥ 100KB | 存到 `data/images/`，存路径 | `image_paths` | 大图存本地文件，仅本地模式可用 |

### 4.3 双数据库模式

| | 本地版（db.js） | 云端版（db-supabase.js） |
|---|---|---|
| 存储引擎 | sql.js（WASM SQLite） | Supabase PostgreSQL + IndexedDB 缓存 |
| 加载条件 | 本地开发、无网络环境 | 线上 GitHub Pages、已登录 |
| tags 格式 | 逗号分隔字符串 | JSON 数组 |
| 接口 | 需保持一致（函数名、返回格式） | 需保持一致 |
| 同步 | 无 | 双向同步（离线优先，联网后 sync） |

**兼容处理**（db-supabase.js）：
- `normalizeTags(tags)` — 字符串/数组/JSON 统一转为数组
- `normalizeToArray(val)` — 通用值归一化
- `extractTagsFromContent(content)` — tags 为空时从 content 提取 #标签

### 4.4 同步机制

| 机制 | 说明 |
|---|---|
| 离线优先 | 所有写操作先写 IndexedDB 本地缓存 |
| 脏标记 | `markDirty()` 标记变更，30 秒自动保存 + 手动 saveDb() |
| 同步队列 | `sync_queue` IndexedDB store 记录待同步操作 |
| 上行同步 | `syncToCloud()` — 将本地变更推送到 Supabase |
| 下行同步 | `syncFromCloud()` — 从 Supabase 拉取云端数据到本地 |
| 手动触发 | 点击同步状态栏触发 `manualSync()` |
| 状态显示 | ☁️ 已同步 / 🔄 同步中... / 📴 离线 / 📱 本地模式 |

---

## 五、导入导出

### 5.1 导入

| 格式 | 说明 |
|---|---|
| flomo HTML | 选择 flomo 导出的 HTML 文件，解析后批量导入，自动去重（按 content 前 100 字符匹配） |

### 5.2 导出

| 格式 | 说明 |
|---|---|
| JSON 文件 | 导出当前用户全部笔记为 JSON 数组 |
| flomo 格式 | 导出为 flomo 兼容的 ZIP 包（每条笔记一个 HTML 文件） |

**注意**：导出范围为当前用户全部笔记，不按标签/搜索筛选导出。

---

## 六、UI 布局

### 6.1 桌面端（已实现）

```
┌──────────────┬──────────────────────────────────┐
│   侧边栏     │          主区域                   │
│  ┌──────────┐│  ┌──────────────────────────────┐ │
│  │ Noteflow ││  │  顶栏：#标签名 / 全部笔记     │ │
│  │ N条笔记  ││  ├──────────────────────────────┤ │
│  ├──────────┤│  │  编辑器：textarea + 工具栏     │ │
│  │ 搜索框   ││  ├──────────────────────────────┤ │
│  │ 每日回顾 ││  │                              │ │
│  ├──────────┤│  │  笔记卡片 1                   │ │
│  │ 标签列表 ││  │  笔记卡片 2                   │ │
│  │ (树形)   ││  │  笔记卡片 3                   │ │
│  │          ││  │  ...                         │ │
│  │          ││  │                              │ │
│  ├──────────┤│  │  加载更多 / 已加载全部        │ │
│  │导入 导出 ││  └──────────────────────────────┘ │
│  │ 退出登录 ││                                   │
│  └──────────┘│                                   │
└──────────────┴──────────────────────────────────┘
```

**编辑模态框**：全屏遮罩 + 居中卡片，标题"编辑笔记"，textarea 编辑内容，底部取消/保存按钮。

### 6.2 手机端（开发中）

```
┌─────────────────────┐
│  搜索框  │  标签栏   │  ← 顶部固定
├─────────────────────┤
│                     │
│   笔记卡片 1        │
│   笔记卡片 2        │  ← 中间可滚动
│   笔记卡片 3        │
│   ...               │
│                     │
├─────────────────────┤
│ [📷] 输入框... [发送]│  ← 底部固定
└─────────────────────┘
```

**设计规范**：
- 浅色清新风格
- 适配 iPhone 安全区域：`padding-bottom: env(safe-area-inset-bottom)`
- 触摸友好：按钮最小点击区域 44px
- 标签横向滚动

---

## 七、非功能性需求

### 7.1 性能

| 指标 | 要求 |
|---|---|
| 首次加载 | < 3 秒（含 CDN 资源） |
| 笔记列表渲染 | 50 条笔记 < 500ms |
| 搜索响应 | debounce 300ms |
| 图片压缩 | < 2 秒/张 |
| 自动保存 | 30 秒间隔，脏标记触发 |

### 7.2 兼容性

| 环境 | 支持情况 |
|---|---|
| Chrome 90+ | ✅ |
| Safari 15+ | ✅ |
| Firefox 90+ | ✅ |
| 移动端浏览器 | 🔧 开发中 |
| IE | ❌ |

### 7.3 安全

| 项 | 说明 |
|---|---|
| 密码存储 | pgcrypto `crypt()` + `bf` 盐，不明文存储 |
| 密码验证 | Supabase RPC 函数 `verify_password()`，避免明文传输到前端 |
| RLS | notes/tags 表启用行级安全（当前策略为应用层鉴权，ALL USING true） |
| API Key | 仅使用 anon key，service_role key 不出现在前端代码中 |

### 7.4 可用性

| 项 | 说明 |
|---|---|
| 离线可用 | 本地模式/离线模式下所有功能可用（除同步外） |
| 错误提示 | 全局错误捕获，白屏时显示错误信息 |
| Toast 通知 | 操作反馈（保存成功、登录成功等） |

---

## 八、部署与运维

### 8.1 环境信息

| 项 | 值 |
|---|---|
| GitHub 仓库 | `ShawnTianT/noteflow` |
| GitHub Pages | `https://shawntiant.github.io/noteflow/`（main 分支 `/docs` 目录） |
| Supabase 项目 | `rsqamgjvreurggjoxphq.supabase.co` |
| 本地服务器 | `localhost:8765`（Node.js，从 `dist/` 提供页面） |
| 公司网络 | GitHub 443 端口不稳定，push 可能需要代理 |

### 8.2 版本发布流程

1. 修改根目录源文件（`js/`、`css/`、`index.html`）
2. 更新 `VERSION` 文件
3. 运行 `bash build.sh`（同步到 `dist/` + `docs/`，自动修正 mobile/ 引用路径）
4. 本地验证：浏览器访问 `localhost:8765`，强刷 `Cmd+Shift+R`
5. 推线上：`bash build.sh push "提交信息"`
6. 线上验证：访问 `https://shawntiant.github.io/noteflow/`

### 8.3 项目目录规则

- **源文件**（`index.html`、`js/`、`css/`、`mobile/`）：可编辑
- **`dist/`**：本地发布目录，**禁止直接编辑**，由 `build.sh` 生成
- **`docs/`**：线上发布目录，**禁止直接编辑**，由 `build.sh` 生成
- **`libs/`**：第三方库，不要修改
- **`data/`**：预填充数据库 + 图片

---

## 九、版本历史

| 版本 | 日期 | 主要变更 |
|---|---|---|
| 1.0.0 | 2026-05-01 | MVP：输入、标签、时间线、搜索、编辑/删除、图片上传/展示 |
| 1.1.0 | 2026-05-02 | 双用户切换、数据导出、flomo 导入、每日回顾、标签置顶 |
| 1.2.0 | 2026-05-03 | Supabase 云端同步、自定义用户表登录、GitHub Pages 部署 |
| 1.3.0 | 2026-05-03 | DB 接口统一（tags 格式兼容、异步一致）、搜索增强 |
| 1.3.1 | 2026-05-03 | 离线标签计数修复、ID 冲突修复、搜索分词 AND、debounce 清理 |

---

## 十、待规划功能（Backlog）

| 优先级 | 功能 | 说明 |
|---|---|---|
| P0 | 手机端 UI | mobile/ 骨架已搭建，UI 待实现 |
| P1 | 自助注册 | 当前需管理员手动插入 SQL |
| P1 | 笔记加密 | 敏感内容端到端加密 |
| P2 | Markdown 渲染 | 支持标题/列表/代码块等格式 |
| P2 | 笔记置顶/收藏 | 置顶重要笔记，快速访问 |
| P2 | 标签颜色 | 自定义标签颜色 |
| P3 | 协作共享 | 笔记分享给其他用户 |
| P3 | API 开放 | 提供 REST API 供第三方工具集成 |

---

*文档维护者：凌霄 & AI Agent | 如有更新请同步修改此文档*
